# Um único domínio: /token e /rooms vão para o servidor de token, o resto para o LiveKit.
voz.SEUDOMINIO.com.br {
    @api path /token /rooms /health
    reverse_proxy @api 127.0.0.1:3001
    reverse_proxy 127.0.0.1:7880
}
